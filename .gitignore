# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Dependencies / build output (if tooling gets added later)
node_modules/
dist/
.netlify/

# Environment & secrets
.env
.env.local

# Internal planning & personal documents — not for the repo
# (contains Henry's personal contact info, unpublished pricing details,
# and the raw draft of his bio — keep these local only)
*.pdf
!open_items_for_henry.pdf
business_card_front.jpeg
business_card_back.jpeg
questions-for-henry.md
site-change-plan.md
open-items.md

# Reference photos not currently used on the site ("ignore for now")
turtle.jpeg
Wounded_Warrior_Charity.png

# No longer used — Charities section switched to text wordmarks for all
# three orgs (see open-items.md), so this logo file is now orphaned.
images/wounded-warrior-project.png

# Note: images/orca.jpg and images/sextant.jpg ARE tracked — they're
# real site assets referenced in index.html, not internal reference material.

# Raw, uncompressed original — the optimized images/ho_bio_pic.jpg is the
# tracked site asset actually used in index.html. (Couldn't delete this
# original from the workspace folder, so it's ignored instead.)
images/ho_bio_pic.png
